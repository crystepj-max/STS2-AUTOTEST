使用目标模式持续推进以下任务，直到所有 P0 项目完成真实游戏验收，或者确认遇到无法自行解决的外部环境阻塞。

不要只输出分析、方案或代码检查后停止。必须检查当前状态、补检查、修改公共平台、运行自动检查、通过公共 MCP 提交真实任务、读取证据、根据失败继续迭代，最终达到完成标准。

# 一、工作目标

项目：

/Users/chris/STS2-WORKSPACE/STS2-AUTOTEST

本轮只收口 STS2-AUTOTEST 的 P0 通用能力：

1. 修正 NEXT_ACT 的完成条件；
2. 修复截图、日志、证据清单、报告和压缩包的一致性；
3. 确保任务结束后仍能持久读取报告与证据；
4. 使用最终正确的窗口截图能力重新完成一次真实整章验收；
5. 对十类目标场景分别完成公共入口验收，或者返回可验证的不可达结果。

最终能力定义：

给平台一个合法 character_id 和一个支持的 target_scene，平台必须在限定时间内：

- 完成目标并返回可验证结果；或者
- 明确说明卡在哪一步、为什么不能继续，并提供完整证据。

平台不得依赖：

- 具体角色名称；
- Gawain 或其他 Mod 规则；
- 具体卡牌、遗物、事件或资源名称；
- 某个 Agent 的临场判断；
- 项目级 Skill 是否被读取；
- 操作接口返回成功但游戏状态没有变化的假成功。

# 二、当前基线

当前已提交版本：

399c1a3 feat: add generic target scene execution

当前 main 与 origin/main 一致。

现有公共能力包括：

- capabilities
- submit_run
- get_run
- cancel_run
- resume_run
- get_report

现有目标场景包括：

- MAIN_MENU
- CHARACTER_SELECT
- MAP
- EVENT
- COMBAT
- REST
- SHOP
- CHEST
- CARD_REWARD
- NEXT_ACT

现有任务参数包括：

- journey
- character_id
- target_scene
- route_policy
- combat_mode
- timeout
- evidence
- idempotency_key

当前公共 MCP 地址通常为：

http://127.0.0.1:8090/mcp

开始工作时必须重新调用 capabilities，不得仅根据文档假定服务状态。

当前相关检查最近一次结果：

128 passed

对应范围包括：

- 目标场景执行；
- 公共导航；
- MCP 契约；
- 任务生命周期；
- 截图和报告；
- 跨 Agent 公共契约。

默认 pytest 全量收集目前存在两个已有的 generated 测试同名冲突。不得通过删除用户文件掩盖这个问题。本轮可将其作为已知非 P0 问题单独记录，但必须运行与本次修改直接相关的完整检查范围。

# 三、工作区保护

当前工作区可能包含其他 Agent 尚未提交的文档修改和本地内容。

开始前执行：

- git status --short
- git diff --stat
- git log -3 --oneline

必须保留所有已有修改，禁止：

- reset；
- checkout 覆盖；
- 删除生成目录；
- 清理报告；
- 修改或提交 .env；
- 删除 .serena、.workbuddy、reports 或 tests/output；
- 把无关文档改动混入本任务。

只修改完成 P0 所必需的公共平台代码、对应检查和权威文档。

不要提交或推送代码，除非用户后续明确要求。

# 四、已确认的关键问题

## 4.1 NEXT_ACT 假完成

当前统一目标执行器位于：

src/sts2_autotest/core/journeys.py

当前 NEXT_ACT 的实际完成判断主要是：

- 章节编号增加；
- 到达稳定 MAP。

这不足以满足原始整章完成定义。

真实游戏现场已经确认：

- 第一章 Boss 奖励完成后；
- 游戏会进入第二章入口事件；
- 现场状态曾为 act_id=1、floor=18、screen=EVENT；
- 事件为 OROBAS；
- in_combat=false；
- 敌人为空；
- 可执行操作为 choose_event_option。

此前自动任务却在以下状态提前标记 PASSED：

- 章节已变为第二章；
- 短暂 screen=COMBAT；
- 但 in_combat=false；
- combat=null；
- 敌人为空；
- 没有 play_card/end_turn；
- 随后出现 MAP。

这段伪 COMBAT 不能视为真实战斗，也不能代替第二章入口事件。

现有问题证据：

tests/output/verified-evidence-run-20260716-120650-ae4be987/reports/journey-trace.json

现场第二章事件证据：

tests/output/verified-live-second-act-event-20260716/state.json
tests/output/verified-live-second-act-event-20260716/actions.json

## 4.2 证据清单与实际文件冲突

整章任务：

run-20260716-120650-ae4be987

实际压缩包中包含：

- 42 张截图；
- 1 份游戏日志；
- 状态轨迹；
- 机器报告。

实际压缩包：

tests/output/artifacts/run-20260716-120650-ae4be987_passed_20260716T120857.zip

但证据清单却写成：

- screenshot_count=0；
- log_count=0；
- 截图不可用；
- 日志不可用。

证据清单：

tests/output/verified-evidence-run-20260716-120650-ae4be987/reports/evidence-manifest.json

必须定位证据清单生成时机或证据归档顺序，保证最终清单是在所有截图、日志、报告和压缩包都完成后生成。

## 4.3 get_report 不能持久读取证据

当前通过公共 MCP 对上述 run_id 调用 get_report 时，会出现：

- summary 和任务状态仍可读取；
- evidence_manifest 为空；
- journey_trace 为空；
- 返回的 evidence_pack_url 不是实际压缩包路径。

原因与原始运行目录被清理、报告只从原目录读取有关。

公共报告必须支持：

- 原始运行目录仍存在时读取；
- 原始目录被清理后，从持久证据目录或压缩包读取；
- 返回实际存在的压缩包路径；
- 报告中的文件路径都能真实打开；
- 不得返回看似合法但不存在的默认路径。

## 4.4 最终正确截图尚未完成整章闭环

macOS 上最终正确的截图方式是：

- 绑定 STS2 实际窗口；
- 不依赖前台焦点；
- 不按桌面矩形盲截；
- 先从窗口本身取得图像；
- 输出标准 1920×1080 JPEG；
- 图中不能出现桌面、Codex 或其他应用；
- 不能只有上方约 20% 有内容、下方纯黑。

当前正确的现场样本：

tests/output/verified-live-second-act-event-20260716/jpeg-window-bound-api/second-act-entry-event_0.jpg

相关实现：

src/sts2_autotest/evidence/capture.py

但最终正确截图能力还没有重新经过一次完整公共 MCP 整章任务，因此不能把旧整章压缩包作为最终截图验收。

# 五、P0-1：修正 NEXT_ACT 完成条件

## 5.1 新完成定义

NEXT_ACT 必须完成以下完整序列：

1. 从第一章新局开始；
2. 正确选择 character_id；
3. 完成第一章开图事件；
4. 遍历第一章地图；
5. 处理实际进入的所有房间；
6. 完成第一章 Boss；
7. 处理 Boss 奖励；
8. 观察章节编号从第一章变为第二章；
9. 识别第二章入口 EVENT；
10. 保存事件标识、选项和截图；
11. 使用统一事件处理能力完成入口事件；
12. 到达第二章稳定 MAP；
13. 再次确认仍为第二章；
14. 最终才允许 PASSED。

## 5.2 过渡状态规则

以下状态不得被识别为真实 COMBAT：

- screen=COMBAT；
- 但 in_combat=false；
- combat 为空；
- 敌人为空；
- 没有 play_card 或 end_turn。

应将其识别为过渡状态，或至少标记为：

- TRANSITION；
- 非稳定状态；
- 不可作为目标完成凭证。

不得根据一次 MAP 快照立即结束。必须确认：

- 第二章入口事件已经完成；
- 地图状态稳定；
- map.is_traveling=false；
- 没有未处理事件；
- 没有未完成奖励；
- 当前可执行操作与 MAP 一致。

## 5.3 自动检查

先补能复现问题的检查，再修改实现。

至少覆盖：

- Boss 奖励后出现伪 COMBAT；
- 伪 COMBAT 后出现第二章 EVENT；
- 事件多步骤选择；
- EVENT 完成后才出现稳定 MAP；
- 章节变化但事件未完成时不得 PASSED；
- 章节变化且只出现伪 COMBAT 时不得 PASSED；
- 第二章 EVENT 无法操作时返回 FAILED_PLATFORM 和完整证据；
- 第二章 EVENT 操作成功但状态未变化时返回 NO_PROGRESS；
- 第二章稳定 MAP 后才允许 NEXT_ACT PASSED。

# 六、P0-2：证据闭环

## 6.1 截图

根据操作系统选择对应能力。

macOS：

- 必须绑定 STS2 窗口本身；
- 输出标准 JPEG；
- 默认 1920×1080；
- 不依赖窗口是否在前台；
- 不得截取桌面；
- 不得使用只有上方内容、下方纯黑的图片；
- 必须实际读取图片并做视觉检查，不能只检查文件尺寸。

Windows：

- 保留或完善按游戏窗口截图；
- 不得退化为全屏截图；
- 本轮若无法取得 Windows 真实环境，必须明确标记“代码有支持、真实环境未验收”。

关键截图至少包括：

- 第一章开图事件；
- 第一章稳定地图；
- 普通战斗；
- 战斗奖励；
- 营火；
- 商店；
- 宝箱；
- Boss 战；
- Boss 奖励；
- 章节切换过渡；
- 第二章入口事件；
- 第二章稳定地图；
- 失败或恢复现场。

## 6.2 日志

先判断操作系统，再选择日志目录。

macOS 默认日志来源：

~/Library/Application Support/SlayTheSpire2/logs/

Windows 使用项目已有的 Windows 日志定位规则。

成功和失败任务都必须采集日志。

如果日志过大：

- 保存有明确大小上限的尾部快照；
- 记录原始日志来源；
- 记录截取方式和字节数；
- 不得直接把超大日志全部塞入 MCP 响应。

## 6.3 证据清单

最终 evidence-manifest 必须在所有采集、报告和归档完成后生成。

至少记录：

- evidence_level；
- 声明的文件；
- 实际存在的文件；
- screenshot_count；
- log_count；
- 每张截图的格式和尺寸；
- 日志来源和证据文件；
- journey-trace；
- run-result；
- 人类报告；
- 压缩包实际路径；
- 缺失文件和真实原因。

清单声明数量必须与磁盘和压缩包一致。

必须增加自动检查：

- 清单 42 张时压缩包确实有 42 张；
- 清单 1 份日志时压缩包确实有 1 份；
- 文件生成顺序变化不会产生旧清单；
- 任务成功后补采截图和日志，清单会重新生成；
- 压缩包路径不存在时不能返回成功报告。

## 6.4 报告耗时

统一计时口径。

以下位置的耗时必须一致或只存在允许的极小写入差异：

- run 任务记录；
- run-result；
- journey-trace；
- summary；
- JUnit；
- get_report。

不得再出现同一任务相差约 10 秒且没有说明的情况。

# 七、P0-3：get_report 持久读取

公共 get_report 必须成为长期可用的事实入口。

完成标准：

1. 任务完成后立即调用可以读取；
2. MCP 服务重启后仍可读取；
3. 原运行目录被正常清理后仍可读取；
4. 可以定位真实证据压缩包；
5. 可以读取机器报告、证据清单和旅程轨迹；
6. get_run 与 get_report 的最终状态一致；
7. 不返回不存在的默认路径；
8. 报告不能因为嵌入完整巨大状态而被普通 Agent 截断。

建议让 get_report 返回精简摘要和实际证据路径，不要把数十万字符的完整状态轨迹全部内嵌到一次响应中。完整轨迹保存在文件或压缩包中。

必须补检查：

- 服务重启后读取；
- 原目录存在；
- 原目录被清理但压缩包存在；
- 压缩包损坏；
- 清单缺失；
- 报告路径错误；
- 终态不一致；
- 大轨迹响应不会导致主要字段丢失。

# 八、P0-4：十类目标场景独立验收

必须通过同一个公共 MCP 入口分别提交，不允许内部函数代替。

每次都必须执行：

submit_run
→ 保存 run_id
→ get_run 观察进展
→ 进入终态
→ get_report
→ 核对证据

目标矩阵：

## MAIN_MENU

成功标准：

- 当前为主菜单；
- 可以开始新局；
- 旧局不会阻塞。

## CHARACTER_SELECT

成功标准：

- 进入角色选择；
- character_id 合法；
- 目标角色可被识别；
- 不要求依赖角色显示名称。

## EVENT

成功标准：

- 真实 EVENT；
- 有事件标识或事件状态；
- 有未锁定选项或合法推进动作；
- 截图可用。

## MAP

成功标准：

- 真实稳定地图；
- is_traveling=false；
- 没有未完成事件或奖励；
- 可读取当前坐标和可走节点。

## COMBAT

成功标准：

- in_combat=true；
- 敌人不为空；
- 有真实战斗动作，或明确公开平台级 win_combat；
- 伪 COMBAT 不得通过。

## REST

成功标准：

- 真实营火；
- 有合法营火选项；
- 状态和截图完整。

## SHOP

成功标准：

- 真实商店；
- 可正常离开；
- 不要求购买。

## CHEST

成功标准：

- 真实宝箱；
- 可开箱并处理必要奖励；
- 页面状态和截图完整。

## CARD_REWARD

成功标准：

- 真实奖励或选牌页面；
- 有奖励数据；
- 有选择、领取、跳过或继续动作；
- 不能把空奖励过渡态视为通过。

## NEXT_ACT

必须满足第五部分的完整定义。

如果某个目标在当前随机地图确实不可达：

- 返回结构化 TARGET_UNREACHABLE；
- 保存地图和路线；
- 记录当前场景、可走节点和最近操作；
- 不能用其他房间冒充；
- 不能无限等待。

# 九、角色和 Mod 边界

本轮主要角色可继续使用 IRONCLAD，以减少变量。

至少再使用 SILENT 完成一次短目标，确认平台没有因本轮修改重新写死角色。

不得加入：

- 角色名称分支；
- 具体卡牌名称分支；
- Gawain 卡牌或遗物规则；
- Gawain 专属调试命令；
- 项目专用恢复脚本；
- 为某次随机地图写死节点。

允许使用平台级开发能力：

- win_combat

但仅限：

- combat_mode=traversal；
- 当前环境明确公开；
- 报告明确说明这是通路验证；
- 使用后仍验证战斗离开、敌人清空和后续页面。

# 十、真实游戏启动与安全规则

启动或恢复游戏时必须使用现有 sts2-start-game skill。

每次启动前先确认：

- 当前是否已有 STS2 进程；
- 8080 是否已监听；
- health 是否可读；
- state 是否可读；
- actions 是否可读。

进程存在不等于游戏可操作。

如果游戏已经运行且健康：

- 复用现有实例；
- 不要重复启动。

如果必须重启：

1. 先确认旧游戏已退出；
2. 确认 8080 已释放；
3. 再启动一次；
4. 不允许重复触发 Steam；
5. 避免再次产生多个 WaitingPrevProcess 弹窗。

公共 MCP 也只能保留一个 8090 服务实例。

若 traversal 依赖开发调试能力，确认公共 MCP 和游戏任务都继承正确配置。不得因为重启服务而丢失 win_combat 或安全复位能力。

# 十一、强制迭代规则

真实任务失败后：

1. 读取 get_run；
2. 读取 get_report；
3. 打开最后状态；
4. 查看最后截图；
5. 查看日志；
6. 明确最后成功场景；
7. 明确失败场景；
8. 明确最近操作；
9. 判断是环境、平台、目标不可达还是产品问题；
10. 先增加能复现该问题的检查；
11. 再修公共能力；
12. 运行小范围检查；
13. 运行相关完整检查；
14. 使用新幂等键重新提交。

每次真实重试必须：

- 使用新 run_id；
- 使用新 idempotency_key；
- 不覆盖旧证据；
- 不删除失败历史；
- 不降低验收标准。

# 十二、禁止事项

- 禁止只修改文档后宣布完成；
- 禁止只通过自动检查后宣布真实验收完成；
- 禁止使用内部函数替代公共 MCP；
- 禁止用一次性脚本直接推进游戏；
- 禁止人工点击帮助任务通过；
- 禁止把伪 COMBAT 当作真实战斗；
- 禁止把章节编号变化单独当作 NEXT_ACT 完成；
- 禁止跳过第二章入口事件；
- 禁止把不可用截图写成已截图；
- 禁止把不存在的路径返回给其他 Agent；
- 禁止把压缩包中存在的文件在清单中写成 0；
- 禁止写死 IRONCLAD、SILENT 或 Gawain；
- 禁止修改 Gawain 项目业务代码；
- 禁止覆盖当前工作区已有修改；
- 禁止提交或推送，除非用户明确要求。

# 十三、自动检查要求

至少执行：

1. 新增问题的精确检查；
2. 目标场景相关单元检查；
3. journeys/navigation 检查；
4. MCP/run_service 检查；
5. capture/log/report/evidence 检查；
6. cross-agent contract 检查；
7. compileall；
8. git diff --check；
9. import 边界检查。

默认 pytest 若仍被已有 generated 同名文件阻断：

- 不得删除文件；
- 记录原始错误；
- 使用项目安全边界执行其余完整检查；
- 将生成测试冲突单独列为非 P0 遗留问题。

# 十四、最终完成门槛

只有同时满足以下条件，P0 才能标记为 PASSED：

1. NEXT_ACT 强制处理第二章入口事件；
2. 伪 COMBAT 不再被识别为真实战斗；
3. 第二章事件完成后才到稳定地图；
4. 十类目标场景均有独立公共任务结果；
5. 可达目标真实通过；
6. 不可达目标返回结构化原因和证据；
7. 最终正确窗口截图经过完整整章任务验证；
8. 截图不是桌面、不是上方小块加下方黑屏；
9. macOS 日志实际进入证据包；
10. 证据清单数量与压缩包一致；
11. get_report 返回真实存在的报告和压缩包；
12. MCP 服务重启后仍能读取历史报告；
13. 原运行目录清理后仍能读取历史报告；
14. 报告耗时口径一致；
15. IRONCLAD 完成完整第一章到第二章稳定地图；
16. SILENT 完成至少一个短目标；
17. 公共路径没有新增角色或 Mod 专属规则；
18. 相关自动检查全部通过；
19. 旧失败证据和用户已有修改全部保留；
20. 最终整章任务状态为 PASSED。

# 十五、最终回传格式

## 总体结论

- P0 总体状态：PASSED / PARTIAL / FAILED / BLOCKED
- NEXT_ACT 完成条件：
- 第二章入口事件：
- 证据闭环：
- 持久报告：
- 十类目标场景：
- 最终截图：
- 最终整章：
- 是否使用公共 MCP：
- 是否包含角色或 Mod 专属规则：

## 修改内容

- 修改文件：
- 每项修改解决的问题：
- 原有工作区内容如何保留：

## 自动检查

- 精确问题检查：
- 目标场景检查：
- MCP 和任务检查：
- 证据和报告检查：
- 完整安全回归：
- 跳过项：
- 跳过原因：

## 十类目标场景验收

每个目标列出：

- target_scene：
- run_id：
- character_id：
- 最终状态：
- 最终场景：
- 耗时：
- 失败原因：
- 报告：
- 证据包：

## 最终整章运行

- run_id：
- character_id：
- 起始章节：
- 第一章开图事件：
- 地图路线：
- 实际房间：
- Boss：
- Boss 奖励：
- 章节切换：
- 第二章入口事件：
- 事件选项和操作：
- 第二章稳定地图：
- 总耗时：
- 最终结果：

## 证据一致性

- 截图清单数量：
- 压缩包截图数量：
- 日志清单数量：
- 压缩包日志数量：
- run-result：
- journey-trace：
- evidence-manifest：
- 人类报告：
- 压缩包：
- get_report 返回路径：
- 服务重启后读取：
- 原目录清理后读取：

## 未解决问题

- 非 P0 遗留问题：
- 外部环境风险：
- 是否影响 P0 结论：

## 工作区

- 当前分支：
- 原有未提交内容：
- 本轮新增修改：
- 是否提交代码：否
- 是否推送代码：否

在以上完成门槛全部达到前，不得把 P0 标记为完成。